services:
  traefik:
    image: traefik:v3.6
    container_name: traefik
    restart: unless-stopped
    command:
      - --api.dashboard=false
      - --log.level=INFO
      - --accesslog=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --entrypoints.web.http.redirections.entrypoint.to=websecure
      - --entrypoints.web.http.redirections.entrypoint.scheme=https
      - --entrypoints.web.http.redirections.entrypoint.permanent=true
      - --certificatesresolvers.le.acme.email=${LETSENCRYPT_EMAIL}
      - --certificatesresolvers.le.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.le.acme.httpchallenge=true
      - --certificatesresolvers.le.acme.httpchallenge.entrypoint=web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/acme.json:/letsencrypt/acme.json
    networks:
      - edge
      - backend

  n8n:
    image: ${N8N_IMAGE}
    container_name: n8n
    restart: unless-stopped
    environment:
      - N8N_HOST=${DOMAIN_N8N}
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - WEBHOOK_URL=https://${DOMAIN_N8N}/
      - N8N_PROXY_HOPS=1
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - GENERIC_TIMEZONE=${TZ}
      - TZ=${TZ}
      - NODE_ENV=production
    volumes:
      - ./n8n_data:/home/node/.n8n
    depends_on:
      - qdrant
      - rsshub
    labels:
      - traefik.enable=true
      - traefik.http.routers.n8n.rule=Host(`${DOMAIN_N8N}`)
      - traefik.http.routers.n8n.entrypoints=web,websecure
      - traefik.http.routers.n8n.tls=true
      - traefik.http.routers.n8n.tls.certresolver=le
      - traefik.http.routers.n8n.tls.domains[0].main=${DOMAIN_N8N}
      - traefik.http.routers.n8n.tls.domains[0].sans=${DOMAIN_QDRANT},${DOMAIN_RSSHUB}
      - traefik.http.services.n8n.loadbalancer.server.port=5678
    networks:
      - edge
      - backend

  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    restart: unless-stopped
    volumes:
      - ./qdrant_data:/qdrant/storage
    labels:
      - traefik.enable=true
      - traefik.http.routers.qdrant.rule=Host(`${DOMAIN_QDRANT}`)
      - traefik.http.routers.qdrant.entrypoints=web,websecure
      - traefik.http.routers.qdrant.tls=true
      - traefik.http.services.qdrant.loadbalancer.server.port=6333
    networks:
      - edge
      - backend

  rsshub:
    image: diygod/rsshub:latest
    container_name: rsshub
    restart: unless-stopped
    environment:
      - PORT=1200
      - LISTEN_INADDR_ANY=1
    labels:
      - traefik.enable=true
      - traefik.http.routers.rsshub.rule=Host(`${DOMAIN_RSSHUB}`)
      - traefik.http.routers.rsshub.entrypoints=web,websecure
      - traefik.http.routers.rsshub.tls=true
      - traefik.http.services.rsshub.loadbalancer.server.port=1200
    networks:
      - edge
      - backend

networks:
  edge:
  backend: